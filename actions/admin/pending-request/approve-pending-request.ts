"use server"
 /* //TODO when user approves the request then in student model courses course id modules approved at date and isApproved is set to true in batches batchId is added mode is added also the enrollment date After than in course model in batch model and in modules model the student id is added we are getting email as a parameter using it we have to find the student id After that the first invoice for student is created using totalFee and if fee status is paid then amount will be same as total fee also remaining fee is calculated based in these and all the remaining felids will be added */
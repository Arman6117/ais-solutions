import React from "react";

type EditInstructorDialogProps = {
  open: boolean;
  onClose: () => void;
};
const EditInstructorDialog = ({
  open,

  onClose,
}: EditInstructorDialogProps) => {
  return <div>EditInstructorDialog</div>;
};

export default EditInstructorDialog;
